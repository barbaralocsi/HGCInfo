<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="4dp">

    <ImageView
        android:id="@+id/player_row_photo"
        android:layout_width="@dimen/small_player_image_width"
        android:layout_height="@dimen/small_player_image_height"
        tools:src="@android:drawable/ic_menu_camera"
        android:layout_gravity="center"
        />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="16dp"
        android:layout_marginLeft="16dp"
        android:orientation="vertical"
        android:layout_weight="1">

        <TextView
            android:id="@+id/player_row_nick"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="NickName"
            android:textStyle="bold"
            />

        <TextView
            android:id="@+id/player_row_real"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="RealName" />
    </LinearLayout>

    <ImageView
        android:id="@+id/player_row_role"
        android:layout_width="@dimen/role_image_width"
        android:layout_height="@dimen/role_image_height"
        tools:src="@android:drawable/ic_menu_camera"
        android:layout_gravity="center"
        />

</LinearLayout>